from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Test to see if I can make a predictive maintenance work for a company',
    author='Adrián E',
    license='MIT',
)
